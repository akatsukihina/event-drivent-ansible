---
- name: Clear all cache
  uri:
    url: "http://{{ es_master }}:9200/_cache/clear"
    method: POST
    user: "{{ es.username }}"
    password: "{{ es.password }}"
    force_basic_auth: true
    status_code: 200

- name: Show result
  debug:
    msg: "Elasticsearch cache cleared."
