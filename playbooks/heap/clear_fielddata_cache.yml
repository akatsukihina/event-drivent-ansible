---
- name: Clear fielddata cache
  uri:
    url: "http://{{ es_master }}:9200/_cache/clear?fielddata=true"
    method: POST
    user: "{{ es.username }}"
    password: "{{ es.password }}"
    force_basic_auth: true
    status_code: 200

- debug:
    msg: "Fielddata cache cleared."
